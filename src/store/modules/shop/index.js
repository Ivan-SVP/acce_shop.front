import productList from './productList';
import productDetail from './productDetail';
import cart from './cart';

const modules = {
    productList,
    productDetail,
    cart,
}
export default {
    namespaced: true,
    modules,
}